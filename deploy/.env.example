# Copy this file to deploy/.env and fill in the values.
# deploy/.env is gitignored — never commit real secrets.

# Contents of config/master.key (required by Rails encrypted credentials)
RAILS_MASTER_KEY=

# Strong password for the p2 PostgreSQL user
# Generate one: openssl rand -hex 32
P2_DATABASE_PASSWORD=
